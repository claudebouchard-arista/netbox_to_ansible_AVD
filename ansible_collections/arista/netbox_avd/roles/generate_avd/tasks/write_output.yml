---
- name: Set output directory names
  ansible.builtin.set_fact:
    _dc_name: "{{ netbox_site | upper | replace('-', '_') }}"
    _spine_group: "{{ netbox_site | upper | replace('-', '_') }}_SPINES"
    _l3leaf_group: "{{ netbox_site | upper | replace('-', '_') }}_L3_LEAVES"
    _l2leaf_group: "{{ netbox_site | upper | replace('-', '_') }}_L2_LEAVES"

- name: Create output directory structure
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: "0755"
  loop:
    - "{{ avd_output_dir }}"
    - "{{ avd_output_dir }}/group_vars/FABRIC"
    - "{{ avd_output_dir }}/group_vars/{{ _dc_name }}"
    - "{{ avd_output_dir }}/group_vars/{{ _spine_group }}"
    - "{{ avd_output_dir }}/group_vars/{{ _l3leaf_group }}"
    - "{{ avd_output_dir }}/group_vars/{{ _l2leaf_group }}"
    - "{{ avd_output_dir }}/group_vars/NETWORK_SERVICES"
    - "{{ avd_output_dir }}/group_vars/CONNECTED_ENDPOINTS"

- name: Write inventory.yml
  ansible.builtin.copy:
    content: "---\n{{ _avd_inventory | to_nice_yaml(indent=2) }}"
    dest: "{{ avd_output_dir }}/inventory.yml"
    mode: "0644"

- name: Write fabric variables
  ansible.builtin.copy:
    content: "---\n{{ _avd_fabric_defaults | to_nice_yaml(indent=2) }}"
    dest: "{{ avd_output_dir }}/group_vars/FABRIC/fabric_variables.yml"
    mode: "0644"
  when: _avd_fabric_defaults | length > 0

- name: Write Ansible connectivity settings
  ansible.builtin.copy:
    content: "---\n{{ _avd_connectivity_defaults | to_nice_yaml(indent=2) }}"
    dest: "{{ avd_output_dir }}/group_vars/FABRIC/fabric_ansible_connectivity.yml"
    mode: "0644"
  when: _avd_connectivity_defaults | length > 0

- name: Write DC settings
  ansible.builtin.copy:
    content: "---\n{{ _avd_dc_defaults | to_nice_yaml(indent=2) }}"
    dest: "{{ avd_output_dir }}/group_vars/{{ _dc_name }}/{{ _dc_name | lower }}.yml"
    mode: "0644"
  when: _avd_dc_defaults | length > 0

- name: Write spine group_vars
  ansible.builtin.copy:
    content: "---\n{{ _avd_spines_output | to_nice_yaml(indent=2) }}"
    dest: "{{ avd_output_dir }}/group_vars/{{ _spine_group }}/spines.yml"
    mode: "0644"

- name: Write L3 leaf group_vars
  ansible.builtin.copy:
    content: "---\n{{ _avd_l3leaves_output | to_nice_yaml(indent=2) }}"
    dest: "{{ avd_output_dir }}/group_vars/{{ _l3leaf_group }}/l3_leaves.yml"
    mode: "0644"

- name: Write L2 leaf group_vars
  ansible.builtin.copy:
    content: "---\n{{ _avd_l2leaves_output | to_nice_yaml(indent=2) }}"
    dest: "{{ avd_output_dir }}/group_vars/{{ _l2leaf_group }}/l2_leaves.yml"
    mode: "0644"

- name: Write network services
  ansible.builtin.copy:
    content: "---\n{{ _avd_network_services | to_nice_yaml(indent=2) }}"
    dest: "{{ avd_output_dir }}/group_vars/NETWORK_SERVICES/network_services.yml"
    mode: "0644"
  when: _avd_network_services | length > 0

- name: Write connected endpoints
  ansible.builtin.copy:
    content: "---\n{{ _avd_connected_endpoints | to_nice_yaml(indent=2) }}"
    dest: "{{ avd_output_dir }}/group_vars/CONNECTED_ENDPOINTS/connected_endpoints.yml"
    mode: "0644"
  when: _avd_connected_endpoints | length > 0

- name: Write build.yml playbook
  ansible.builtin.copy:
    content: |
      ---
      - name: Build Configurations and Documentation
        hosts: FABRIC
        gather_facts: false
        tasks:

          - name: Generate AVD Structured Configurations and Fabric Documentation
            ansible.builtin.import_role:
              name: arista.avd.eos_designs

          - name: Generate Device Configurations and Documentation
            ansible.builtin.import_role:
              name: arista.avd.eos_cli_config_gen
    dest: "{{ avd_output_dir }}/build.yml"
    mode: "0644"
  when: avd_generate_playbooks | bool

- name: Write deploy.yml playbook
  ansible.builtin.copy:
    content: |
      ---
      - name: Deploy Configurations to Devices using eAPI
        hosts: FABRIC
        gather_facts: false
        tasks:

          - name: Deploy Configurations to Devices
            ansible.builtin.import_role:
              name: arista.avd.eos_config_deploy_eapi
    dest: "{{ avd_output_dir }}/deploy.yml"
    mode: "0644"
  when: avd_generate_playbooks | bool

- name: Display output location
  ansible.builtin.debug:
    msg: "AVD inventory written to {{ avd_output_dir }}/"
